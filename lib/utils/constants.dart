import 'dart:ui';

class AppConstants {
  static const primaryColorLight = Color.fromARGB(255, 232, 236, 235);
  static const backgroundColorLight = Color.fromARGB(255, 30, 30, 30);
}

enum MusicItemTileType {
  list,
  grid,
}
